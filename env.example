# انسخ إلى .env.local (تطوير) أو .env.production (إنتاج)

# مطلوب في الإنتاج — openssl rand -hex 32
AUTH_SECRET=

# الرابط العام بدون / في النهاية — مثل https://book.yourclinic.com
NEXT_PUBLIC_APP_URL=

# أول تشغيل في الإنتاج فقط — 10+ أحرف
INITIAL_ADMIN_PASSWORD=

NODE_ENV=production
